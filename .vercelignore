# Internal working docs live in git but never reach the deployed site.
PROOF-OF-WORK.md

# Agent skills are tooling for building the site, not part of it.
.agents/
skills-lock.json

# Blog sources and the script that renders them. Vercel runs no build command —
# it serves the HTML in blog/, which build.js writes and git tracks.
posts/
build.js
WRITING.md

# Local authoring tooling for the project previews. Vercel serves committed
# HTML and runs no build; shipping a package.json would make it try to.
package.json
package-lock.json
node_modules/
scripts/
projects.json
